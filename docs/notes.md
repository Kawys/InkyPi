# Image Writers
1. PlaylistRefresh.execute() - writes to static/images/plugins/. <br>
   Used by plugin.py:image()
2. MockDisplay.display() - writes to mock_display_output
3. DisplayManager.display_image() - writes to static/images/current_image.png <br>
  Used by main.py:get_current_image()

